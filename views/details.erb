<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h4>Post details</h4>
    <p>
      <%= @row['content'] %>
      <br />
      <br />
      <span style="font-size: 75%; color: #999"> Added at <%= @row['created_date'] %><span>
      <br />
    </p>
    <br />
    <h4><b>Comments:</b></h4>
    <br />
    <% @comments.each do |comment| %>
      <p>
        <%= comment['content'] %>
        <br />
        <br />
        <span style="font-size: 75%; color: #999"> Added at <%= comment['created_date'] %><span>
      </p>
      <hr />
    <% end %>
    <form action="/details/<%= @row['id'] %>" method="POST" role="form">
        <textarea class="form-control" name="content" rows="6" placeholder="Add your comment"></textarea>
      <br>
  <button type="submit" class="btn btn-primary">Add comment</button>
</form>
  </body>
</html>
